---
description: Verify 1C platform and language facts against the local platform help pack
globs:
alwaysApply: false
---

- Treat platform API or syntax claims as unverified until checked in the local `platform` pack.
- Use exact lookup first for `Type.Method`, built-in object names, or syntax topics.
- Separate confirmed facts from inferences.
- Do not answer platform questions from configuration code when the platform help pack can verify them directly.
